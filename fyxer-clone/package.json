{
  "name": "fyxer-clone",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "npm -w packages/shared run build && npm -w apps/functions run build && npm -w apps/web run build",
    "lint": "echo \"add your linter here\"",
    "dev:functions": "npm -w apps/functions run dev",
    "dev:web": "npm -w apps/web run dev",
    "emulators:kill": "bash scripts/kill_firebase_emulators.sh",
    "emulators:start:clean": "npm run emulators:kill && firebase emulators:start",
    "deploy:functions": "npm -w packages/shared run build && npm -w apps/functions run build && firebase deploy --only functions"
  }
}
