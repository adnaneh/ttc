# Local-only environment overrides for Functions emulator.
# Copy to `.env.local` and fill values for local runs.
# NOTE: These are NOT used in deployments.

# Provide your OpenAI key locally only. In production, it must be set via
# Firebase Secret Manager: `firebase functions:secrets:set OPENAI_API_KEY`.
OPENAI_API_KEY=REPLACE_ME

