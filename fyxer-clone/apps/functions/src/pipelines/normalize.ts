// placeholder – classify & extract features
export async function normalizeMessage(messageId: string) {
  // TODO: load from Firestore/Storage, extract senderDomain, intent, urgency, etc.
  return { messageId };
}

